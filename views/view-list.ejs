<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View List</title>
    <link rel="stylesheet" href="/styles/view-list.css">
</head>
<body>
    <h1>List Items</h1>
    <ul>
        <% listItems.forEach(function(item) { %>
            <li>
                <img src="<%= item.image_url %>" alt="Image for <%= item.response_code %>">
                <p>Response Code: <%= item.response_code %></p>
            </li>
        <% }); %>
    </ul>

    <button id="backToSavedListsBtn" onclick="window.location.href='/lists'">Back to Saved Lists</button>

    <script>
        // Add any necessary JavaScript if you want to enhance the view functionality.
    </script>
</body>
</html>
